FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV AGUI_RELOAD=false
# Serve the merged single FastHTML app (landing + auth + chat + guide + voice).
ENV ASSETHERO_WEB_PORT=5003
# Autonomy worker runs as a daemon thread inside the web process (app.py
# bootstraps it). Set AUTONOMY_ENABLED=false in Coolify env to disable.
ENV AUTONOMY_ENABLED=true

EXPOSE 5003

HEALTHCHECK --interval=30s --timeout=10s --start-period=15s --retries=3 \
    CMD python -c "import requests; requests.get('http://localhost:5003/')"

CMD ["python", "main.py"]
